<%= form_for(current_user.active_participations.build) do |form| %>
  <%= hidden_field_tag :subject_id, @subject.id %>
  <%= form.submit "participate", class: "btn btn-primary pull-center" %>
<% end %>
