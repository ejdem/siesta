<% provide(:title, @user.name) %>
<div class="col-md-6 col-md-offset-3"
    <% if !@user.tutor? %>
        <%= render 'participations' %>
    <% else %>
        <p>good moring mister <%= @user.name %></p>
    <% end %>
</div>